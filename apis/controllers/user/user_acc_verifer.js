var email = require('../../../lib/emails/nodemailer');
var model = require('../../models/users');
var emailTemplate = require('../../email-templates/verification');

module.exports = {
    initiateVerification: function( email ) {

    },
    verify: function( userEmail ) {
        
    }
}